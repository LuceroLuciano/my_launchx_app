// Prueba de unidad de la clase MissionComander

describe("Esto es una suite de pruebas", () => {
    test('Caso de prueba 1', () => {
        const result = 1 + 2
        expect(result).toBe(3); // Sustituir  3 por 10 o cualquier otro numero para que la prueba falle 
    });
})
